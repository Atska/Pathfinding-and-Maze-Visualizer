import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#003153",
      contrastText: "#FFFFFF",
    },
    secondary: {
      main: "#f48fb1",
    },
  },
});

export default theme;
